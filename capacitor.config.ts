import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.mdcr.mdcr_learn',
  appName: 'MDCR Learn',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
