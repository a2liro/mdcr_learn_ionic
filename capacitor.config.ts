import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.mdcrlearn',
  appName: 'mdcrlearn',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
